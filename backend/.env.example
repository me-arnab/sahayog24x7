# Port configuration (default is 5000)
PORT=5000

# MongoDB Connection String (optional, defaults to fallback URI in src/config/db.js)
MONGODB_URI=mongodb+srv://arnabme2005_db_user:Qlaf2UNT6RQ2iKBy@complains.quakjti.mongodb.net/?appName=Complains

# JWT settings
JWT_SECRET=sahayog24x7_jwt_fallback_secret_2024
JWT_EXPIRES_IN=7d

# Nodemailer SMTP settings (Optional - for forwarding Contact Us messages to admin mail)
# For Gmail, configure EMAIL_USER to your Gmail and EMAIL_PASS to a Gmail App Password
EMAIL_USER=your-gmail-address@gmail.com
EMAIL_PASS=your-gmail-app-password
